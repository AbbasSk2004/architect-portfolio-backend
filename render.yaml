services:
  - type: web
    name: architect-portfolio-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      # Add your MongoDB URI in Render dashboard
      # Add your FRONTEND_URL in Render dashboard

